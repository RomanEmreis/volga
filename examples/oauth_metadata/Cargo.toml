[package]
name = "oauth_metadata"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
volga = { path = "../../volga", features = ["jwt-auth"] }
serde = { workspace = true, features = ["derive"] }

[lints]
workspace = true
