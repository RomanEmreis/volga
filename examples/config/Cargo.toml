[package]
name = "config"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
volga = { path = "../../volga", features = ["config"]  }
serde = { version = "1.0.228", features = ["derive"] }
tokio = "1.50.0"

[lints]
workspace = true
