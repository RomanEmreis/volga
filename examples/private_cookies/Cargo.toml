[package]
name = "private_cookies"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
uuid = { workspace = true, features = ["v4"] }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
volga = { path = "../../volga", features = ["cookie","private-cookie","di"] }

[lints]
workspace = true
