[package]
name = "request_validation"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
volga = { path = "../../volga", features = ["middleware"] }

[lints]
workspace = true
